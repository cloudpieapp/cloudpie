## CloudPie refresh

- Replace every visible BingBloom name with CloudPie, including web and native app labels.
- Use the uploaded cloud-and-sprout image for the header, menus, loading screen, web icons, and native app icons.
- Replace the current red accent tokens and remaining red effects with the logo’s blue-purple accent.
- Diagnose and repair the blank opening page so `/` loads the existing streaming home screen.
- Verify the opening page and branding at desktop and mobile sizes.

### Technical details

- Keep existing content, navigation, and functionality unchanged.
- Preserve existing URLs and package identifiers so links and installed-app updates do not break.
